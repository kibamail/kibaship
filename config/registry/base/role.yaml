apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: registry-secret-reader
  namespace: registry
rules:
  # Allow init container to check if registry-registry-auth secret exists
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get"]
    resourceNames: ["registry-registry-auth"]