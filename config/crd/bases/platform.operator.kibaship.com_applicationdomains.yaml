---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: applicationdomains.platform.operator.kibaship.com
spec:
  group: platform.operator.kibaship.com
  names:
    kind: ApplicationDomain
    listKind: ApplicationDomainList
    plural: applicationdomains
    singular: applicationdomain
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.domain
      name: Domain
      type: string
    - jsonPath: .spec.port
      name: Port
      type: integer
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.default
      name: Default
      type: boolean
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.certificateReady
      name: Certificate Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ApplicationDomainSpec defines the desired state of ApplicationDomain
            properties:
              applicationRef:
                description: ApplicationRef references the parent application
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              default:
                default: false
                description: |-
                  Default indicates if this is the default domain for the application
                  Only one domain per application can be marked as default
                type: boolean
              domain:
                description: Domain is the full domain name (e.g., "my-app-abc123.myapps.kibaship.com"
                  or "custom.example.com")
                pattern: ^[a-z0-9]([a-z0-9-]*[a-z0-9])?(\.[a-z0-9]([a-z0-9-]*[a-z0-9])?)*$
                type: string
              port:
                default: 3000
                description: Port is the application port for ingress routing
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              tlsEnabled:
                default: true
                description: |-
                  TLSEnabled indicates if TLS/SSL should be enabled for this domain
                  Note: omit 'omitempty' so that false is preserved over the default.
                type: boolean
              type:
                allOf:
                - enum:
                  - default
                  - custom
                - enum:
                  - default
                  - custom
                default: default
                description: Type indicates if this is a default generated domain
                  or custom domain
                type: string
            required:
            - applicationRef
            - domain
            - port
            - tlsEnabled
            type: object
          status:
            description: ApplicationDomainStatus defines the observed state of ApplicationDomain
            properties:
              certificateReady:
                description: CertificateReady indicates if the TLS certificate is
                  ready
                type: boolean
              certificateRef:
                description: CertificateRef references the cert-manager Certificate
                  created for this domain
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the domain state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dnsConfigured:
                description: DNSConfigured indicates if DNS is properly configured
                  (for custom domains)
                type: boolean
              ingressReady:
                description: IngressReady indicates if the ingress is configured and
                  ready
                type: boolean
              lastReconcileTime:
                description: LastReconcileTime is the last time the domain was reconciled
                format: date-time
                type: string
              message:
                description: Message provides human-readable status information
                type: string
              phase:
                allOf:
                - enum:
                  - Pending
                  - Ready
                  - Failed
                - enum:
                  - Pending
                  - Ready
                  - Failed
                description: Phase indicates the current phase of the domain
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
