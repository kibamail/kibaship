---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: applications.platform.operator.kibaship.com
spec:
  group: platform.operator.kibaship.com
  names:
    kind: Application
    listKind: ApplicationList
    plural: applications
    singular: application
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.projectRef.name
      name: Project
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Application is the Schema for the applications API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ApplicationSpec defines the desired state of Application.
            properties:
              dockerImage:
                description: DockerImage contains configuration for DockerImage applications
                properties:
                  image:
                    description: Image is the Docker image reference (e.g., nginx:latest,
                      registry.com/org/image:tag)
                    type: string
                  imagePullSecretRef:
                    description: ImagePullSecretRef references the secret containing
                      image pull credentials
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  tag:
                    description: Tag is the image tag (optional if already specified
                      in Image)
                    type: string
                required:
                - image
                type: object
              environments:
                description: |-
                  Environments defines environment-specific configurations
                  If not specified, a default "production" environment is auto-created by the controller
                items:
                  description: ApplicationEnvironment defines environment-specific
                    configuration
                  properties:
                    gitRepository:
                      description: GitRepository contains environment-specific overrides
                        for GitRepository applications
                      properties:
                        branch:
                          description: Branch override (defaults to application's
                            base branch)
                          type: string
                        buildCommand:
                          description: BuildCommand override
                          type: string
                        startCommand:
                          description: StartCommand override
                          type: string
                      type: object
                    name:
                      description: Name of the environment (production, staging, qa,
                        etc.)
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    replicas:
                      description: Replicas for this environment (optional)
                      format: int32
                      type: integer
                    resources:
                      description: Resources for this environment (optional)
                      properties:
                        claims:
                          description: |-
                            Claims lists the names of resources, defined in spec.resourceClaims,
                            that are used by this container.

                            This field depends on the
                            DynamicResourceAllocation feature gate.

                            This field is immutable. It can only be set for containers.
                          items:
                            description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                            properties:
                              name:
                                description: |-
                                  Name must match the name of one entry in pod.spec.resourceClaims of
                                  the Pod where this field is used. It makes that resource available
                                  inside a container.
                                type: string
                              request:
                                description: |-
                                  Request is the name chosen for a request in the referenced claim.
                                  If empty, everything from the claim is made available, otherwise
                                  only the result of this request.
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - name
                          x-kubernetes-list-type: map
                        limits:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: |-
                            Limits describes the maximum amount of compute resources allowed.
                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                          type: object
                        requests:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: |-
                            Requests describes the minimum amount of compute resources required.
                            If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                            otherwise to an implementation-defined value. Requests cannot exceed Limits.
                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                          type: object
                      type: object
                  required:
                  - name
                  type: object
                type: array
              gitRepository:
                description: GitRepository contains configuration for GitRepository
                  applications
                properties:
                  branch:
                    description: Branch is the git branch to use (optional, defaults
                      to main/master)
                    type: string
                  buildCommand:
                    description: BuildCommand is the command to build the application
                      (optional)
                    type: string
                  env:
                    description: Env is a reference to a secret containing environment
                      variables for this application (optional)
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  path:
                    description: Path is the path within the repository (optional,
                      defaults to root)
                    type: string
                  provider:
                    description: Provider is the Git provider (github.com, gitlab.com,
                      bitbucket.com)
                    enum:
                    - github.com
                    - gitlab.com
                    - bitbucket.com
                    type: string
                  publicAccess:
                    default: false
                    description: |-
                      PublicAccess indicates if the repository is publicly accessible
                      If true, SecretRef is optional. If false, SecretRef is required and must exist in project namespace
                    type: boolean
                  repository:
                    description: Repository is the repository name in the format <org-name>/<repo-name>
                    pattern: ^[a-zA-Z0-9._-]+/[a-zA-Z0-9._-]+$
                    type: string
                  rootDirectory:
                    default: ./
                    description: RootDirectory is the root directory for the application
                      (optional, defaults to ./)
                    type: string
                  secretRef:
                    description: |-
                      SecretRef references the secret containing the git access token
                      Required when PublicAccess is false, optional when PublicAccess is true
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  spaOutputDirectory:
                    description: SpaOutputDirectory is the output directory for SPA
                      builds (optional)
                    type: string
                  startCommand:
                    description: StartCommand is the command to start the application
                      (optional)
                    type: string
                required:
                - provider
                - repository
                type: object
              mysql:
                description: MySQL contains configuration for MySQL applications
                properties:
                  database:
                    description: Database is the initial database name to create
                    type: string
                  secretRef:
                    description: SecretRef references the secret containing MySQL
                      credentials
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  version:
                    description: Version is the MySQL version to deploy
                    type: string
                type: object
              mysqlCluster:
                description: MySQLCluster contains configuration for MySQLCluster
                  applications
                properties:
                  database:
                    description: Database is the initial database name to create
                    type: string
                  replicas:
                    default: 3
                    description: Replicas is the number of MySQL instances in the
                      cluster
                    format: int32
                    minimum: 1
                    type: integer
                  secretRef:
                    description: SecretRef references the secret containing MySQL
                      credentials
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  version:
                    description: Version is the MySQL version to deploy
                    type: string
                type: object
              postgres:
                description: Postgres contains configuration for Postgres applications
                properties:
                  database:
                    description: Database is the initial database name to create
                    type: string
                  secretRef:
                    description: SecretRef references the secret containing PostgreSQL
                      credentials
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  version:
                    description: Version is the PostgreSQL version to deploy
                    type: string
                type: object
              postgresCluster:
                description: PostgresCluster contains configuration for PostgresCluster
                  applications
                properties:
                  database:
                    description: Database is the initial database name to create
                    type: string
                  replicas:
                    default: 3
                    description: Replicas is the number of PostgreSQL instances in
                      the cluster
                    format: int32
                    minimum: 1
                    type: integer
                  secretRef:
                    description: SecretRef references the secret containing PostgreSQL
                      credentials
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  version:
                    description: Version is the PostgreSQL version to deploy
                    type: string
                type: object
              projectRef:
                description: ProjectRef references the Project this application belongs
                  to
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              type:
                description: Type defines the type of application
                enum:
                - MySQL
                - MySQLCluster
                - Postgres
                - PostgresCluster
                - DockerImage
                - GitRepository
                type: string
            required:
            - projectRef
            - type
            type: object
          status:
            description: ApplicationStatus defines the observed state of Application.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the application's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              environmentStatus:
                description: EnvironmentStatus tracks the readiness of each environment
                items:
                  description: EnvironmentStatus tracks the status of an environment
                  properties:
                    lastDeploymentTime:
                      description: LastDeploymentTime tracks last deployment to this
                        environment
                      format: date-time
                      type: string
                    name:
                      description: Name of the environment
                      type: string
                    secretReady:
                      description: SecretReady indicates if environment secret exists
                      type: boolean
                  required:
                  - name
                  - secretReady
                  type: object
                type: array
              message:
                description: Message provides additional information about the current
                  status
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed Application
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the application
                  lifecycle
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
