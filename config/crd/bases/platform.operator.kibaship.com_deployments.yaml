---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: deployments.platform.operator.kibaship.com
spec:
  group: platform.operator.kibaship.com
  names:
    kind: Deployment
    listKind: DeploymentList
    plural: deployments
    singular: deployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.applicationRef.name
      name: Application
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.currentPipelineRun.name
      name: Current Run
      type: string
    - jsonPath: .status.lastSuccessfulRun.name
      name: Last Success
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Deployment is the Schema for the deployments API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DeploymentSpec defines the desired state of Deployment.
            properties:
              applicationRef:
                description: ApplicationRef references the Application this deployment
                  belongs to
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              gitRepository:
                description: |-
                  GitRepository contains configuration for GitRepository deployments
                  Required when ApplicationRef points to a GitRepository application
                properties:
                  branch:
                    description: Branch is the git branch to use (optional, defaults
                      to application branch)
                    type: string
                  commitSHA:
                    description: CommitSHA is the specific commit hash to deploy
                    type: string
                required:
                - commitSHA
                type: object
            required:
            - applicationRef
            type: object
          status:
            description: DeploymentStatus defines the observed state of Deployment.
            properties:
              builtImage:
                description: Built image information from the last successful run
                properties:
                  builtAt:
                    description: Build timestamp
                    format: date-time
                    type: string
                  commitSha:
                    description: Git commit SHA used for this image
                    type: string
                  digest:
                    description: Image digest
                    type: string
                  reference:
                    description: Full image reference with digest
                    type: string
                  tag:
                    description: Image tag
                    type: string
                required:
                - builtAt
                - commitSha
                - digest
                - reference
                - tag
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the deployment's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentPipelineRun:
                description: Current pipeline run information
                properties:
                  commitSha:
                    description: Git commit SHA that triggered this run
                    type: string
                  completionTime:
                    description: Completion time
                    format: date-time
                    type: string
                  image:
                    description: Built image from this run
                    type: string
                  message:
                    description: Message provides additional information about the
                      pipeline run
                    type: string
                  name:
                    description: Name of the pipeline run
                    type: string
                  namespace:
                    description: Namespace of the pipeline run
                    type: string
                  phase:
                    description: Phase of the pipeline run
                    type: string
                  startTime:
                    description: Start time
                    format: date-time
                    type: string
                required:
                - name
                - namespace
                - phase
                type: object
              lastSuccessfulRun:
                description: Last successful pipeline run
                properties:
                  commitSha:
                    description: Git commit SHA that triggered this run
                    type: string
                  completionTime:
                    description: Completion time
                    format: date-time
                    type: string
                  image:
                    description: Built image from this run
                    type: string
                  message:
                    description: Message provides additional information about the
                      pipeline run
                    type: string
                  name:
                    description: Name of the pipeline run
                    type: string
                  namespace:
                    description: Namespace of the pipeline run
                    type: string
                  phase:
                    description: Phase of the pipeline run
                    type: string
                  startTime:
                    description: Start time
                    format: date-time
                    type: string
                required:
                - name
                - namespace
                - phase
                type: object
              message:
                description: Message provides additional information about the current
                  status
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed Deployment
                format: int64
                type: integer
              phase:
                description: Current phase of the deployment
                type: string
              runHistory:
                description: Pipeline run history (last 5 runs)
                items:
                  description: PipelineRunStatus tracks pipeline run information
                  properties:
                    commitSha:
                      description: Git commit SHA that triggered this run
                      type: string
                    completionTime:
                      description: Completion time
                      format: date-time
                      type: string
                    image:
                      description: Built image from this run
                      type: string
                    message:
                      description: Message provides additional information about the
                        pipeline run
                      type: string
                    name:
                      description: Name of the pipeline run
                      type: string
                    namespace:
                      description: Namespace of the pipeline run
                      type: string
                    phase:
                      description: Phase of the pipeline run
                      type: string
                    startTime:
                      description: Start time
                      format: date-time
                      type: string
                  required:
                  - name
                  - namespace
                  - phase
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
