apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: registry

resources:
- issuer.yaml
- certificate.yaml
- serviceaccount.yaml
- clusterrole.yaml
- clusterrolebinding.yaml
- deployment.yaml
- service.yaml
images:
- name: registry-auth
  newName: ghcr.io/kibamail/kibaship-operator-registry-auth
  newTag: v0.1.6
