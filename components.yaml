components:
  # TODO: Database operators removed - will be reimplemented
  # mysql-operator, valkey-operator, postgres-operator configurations removed
  cilium:
    versions:
      - name: 1.18.0
        operator: https://raw.githubusercontent.com/cilium/cilium/v1.18.0/install/kubernetes/quick-install.yaml
